---
import ResumePage from '../../components/ResumePage.astro';

const baseUrl = import.meta.env.BASE_URL;
---

<ResumePage
  locale="en"
  switchHref={baseUrl}
  pdfHref={`${baseUrl}en/pdf/`}
  webHref={`${baseUrl}en/`}
/>
