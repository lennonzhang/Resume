---
import ResumePage from '../../components/ResumePage.astro';

const baseUrl = import.meta.env.BASE_URL;
---

<ResumePage
  locale="zh"
  pdfMode
  switchHref={`${baseUrl}en/pdf/`}
  pdfHref={`${baseUrl}pdf/`}
  webHref={baseUrl}
/>
